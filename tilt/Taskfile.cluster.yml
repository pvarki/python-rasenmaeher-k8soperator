version: "3"

tasks:
  up:
    desc: Start the kind development cluster
    aliases: [u]
    cmds:
      - task: :registry:up
      - bash cluster.sh up

  down:
    desc: Delete the kind development cluster
    aliases: [d]
    cmd: bash cluster.sh down
